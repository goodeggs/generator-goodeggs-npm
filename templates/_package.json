{
  "name": "{{ pkgname }}",
  "version": "0.0.0",
  "description": "{{ description }}",
  "author": "Good Eggs Engineering <eng@goodeggs.com>",
  {{~#if contributors}}
  "contributors": [
    {{#each contributors~}}
    "{{{this}}}"{{#if @index}},{{/if}}
    {{~/each}}
  ],
  {{/if~}}
  "main": "{{underscored pkgname}}",
  "repository": {
    "type": "git",
    "url": "git://github.com/goodeggs/{{ appname }}.git"
  },
  "homepage": "https://github.com/goodeggs/{{ appname }}",
  "bugs": "https://github.com/goodeggs/{{ appname }}/issues",
  "dependencies": {
  },
  "devDependencies": {
    "coffee-script": ">=1.7.x",
    "mocha": "~1.x.x"
  },
  "scripts": {
    "test": "mocha"
  }
}
